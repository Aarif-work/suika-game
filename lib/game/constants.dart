class Constants {
  // Logic dimensions (meters). 
  // 1 meter approx 50-100 logical pixels depending on zoom, but in Forge2D camera handles it.
  // We'll define a resolution for the fixed resolution camera.
  static const double gameWidth = 600;
  static const double gameHeight = 900;
}
